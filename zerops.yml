zerops:
  - setup: api
    build:
      base: nodejs@20
      buildCommands:
        - npm ci
        - npm run build
      deployFiles:
        - bot
        - web  
        - package.json
        - .env
    run:
      base: nodejs@20
      ports:
        - port: 3001
          httpSupport: true
      envVariables:
        NODE_ENV: production
        MC_HOST: donutsmp.net
        MC_PORT: "25565"
        MC_USERNAME: eelhoed_pungent0
        MC_AUTH: microsoft
        PORT: "3001"
      startCommand: npm start